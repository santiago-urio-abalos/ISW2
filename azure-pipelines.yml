trigger:
- main

variables:
  azureServiceConnectionId: 'aefec5e6-8deb-46c7-aab5-9198d93191df'
  webAppName: 'NewHope'
  environmentName: 'surio-v2'
  pythonVersion: '3.11'

stages:

# ===============================================================
# BUILD STAGE
# ===============================================================
- stage: Build
  displayName: Build Stage
  jobs:
  - job: BuildJob
    pool:
      name: Default
      demands:
        - Agent.Name -equals DESKTOP-31M0L0Q

    steps:

    - task: UsePythonVersion@0
      inputs:
        versionSpec: '$(pythonVersion)'
      displayName: "Use Python $(pythonVersion)"

    # ==== Install dependencies and collect static ====
    - powershell: |
        python -m venv env
        .\env\Scripts\Activate.ps1

        python -m pip install --upgrade pip
        pip install -r requirements.txt

        Write-Host "▶ Ejecutando collectstatic usando settings: proyect.settings"
        $env:DJANGO_SETTINGS_MODULE="proyect.settings"
        python manage.py collectstatic --noinput
      displayName: "Install dependencies & collect static"
      workingDirectory: "$(Build.SourcesDirectory)"

    # ==== Prepare deployment folder ====
    - powershell: |
        $deployDir = "$(Build.ArtifactStagingDirectory)\deploy"
        New-Item -ItemType Directory -Path $deployDir -Force

        Write-Host "Copiando proyecto..."
        $include = @("proyect", "relecloud", "reviews", "*.py", "manage.py", "requirements.txt", "static", "staticfiles")

        foreach ($item in $include) {
            Get-ChildItem -Path "$(Build.SourcesDirectory)\$item" -Recurse -ErrorAction SilentlyContinue |
                Copy-Item -Destination $deployDir -Recurse -Force -ErrorAction SilentlyContinue
        }

        Write-Host "Contenido final del ZIP:"
        Get-ChildItem $deployDir -Recurse
      displayName: "Prepare deployment folder"

    - task: ArchiveFiles@2
      displayName: "Create deploy ZIP"
      inputs:
        rootFolderOrFile: '$(Build.ArtifactStagingDirectory)\deploy'
        includeRootFolder: false
        archiveType: zip
        archiveFile: '$(Build.ArtifactStagingDirectory)\deploy.zip'
        replaceExistingArchive: true

    - task: PublishPipelineArtifact@1
      inputs:
        targetPath: '$(Build.ArtifactStagingDirectory)\deploy.zip'
        artifact: 'deploy-artifact'
        publishLocation: 'pipeline'
      displayName: "Publish deploy artifact"


# ===============================================================
# DEPLOY STAGE
# ===============================================================
- stage: Deploy
  displayName: Deploy Web App
  dependsOn: Build
  condition: succeeded()

  jobs:
  - deployment: DeploymentJob
    pool:
      name: Default
      demands:
        - Agent.Name -equals DESKTOP-31M0L0Q

    environment: $(environmentName)

    strategy:
      runOnce:
        deploy:
          steps:

          - task: DownloadPipelineArtifact@2
            inputs:
              buildType: current
              artifactName: deploy-artifact
              targetPath: '$(Pipeline.Workspace)/deploy-artifact'
            displayName: "Download deploy artifact"

          - task: AzureWebApp@1
            displayName: "Deploy Azure Web App"
            inputs:
              azureSubscription: $(azureServiceConnectionId)
              appName: $(webAppName)
              package: '$(Pipeline.Workspace)/deploy-artifact/deploy.zip'
